<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="online.rubick.applications.dao.sys.SysMenuPermissionMapper" >
  <resultMap id="BaseResultMap" type="online.rubick.applications.entity.sys.SysMenuPermission" >
    <result column="menu_id" property="menuId" jdbcType="VARCHAR" />
    <result column="permission_id" property="permissionId" jdbcType="VARCHAR" />
  </resultMap>
  <insert id="insert" parameterType="online.rubick.applications.entity.sys.SysMenuPermission" >
    insert into sys_menu_permission (menu_id, permission_id)
    values (#{menuId,jdbcType=VARCHAR}, #{permissionId,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="online.rubick.applications.entity.sys.SysMenuPermission" >
    insert into sys_menu_permission
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="menuId != null" >
        menu_id,
      </if>
      <if test="permissionId != null" >
        permission_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="menuId != null" >
        #{menuId,jdbcType=VARCHAR},
      </if>
      <if test="permissionId != null" >
        #{permissionId,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <select id="getAll" resultMap="BaseResultMap" >
    select *
    from sys_menu_permission
  </select>
  <select id="getByMenuId" resultMap="BaseResultMap" parameterType="java.lang.String">
    select *
    from sys_menu_permission
    where menu_id = #{menuId,jdbcType=VARCHAR}
  </select>
</mapper>